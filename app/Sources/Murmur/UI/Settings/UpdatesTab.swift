import SwiftUI

struct UpdatesTab: View {
    var body: some View {
        Form {
            Section("Updates") {
                Text("Coming in a later phase.").foregroundStyle(.secondary)
            }
        }
        .padding()
    }
}
