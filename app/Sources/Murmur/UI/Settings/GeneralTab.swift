import SwiftUI

struct GeneralTab: View {
    var body: some View {
        Form {
            Section("General") {
                Text("Coming in a later phase.").foregroundStyle(.secondary)
            }
        }
        .padding()
    }
}
