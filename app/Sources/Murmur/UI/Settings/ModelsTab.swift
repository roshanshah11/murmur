import SwiftUI

struct ModelsTab: View {
    var body: some View {
        Form {
            Section("Models") {
                Text("Coming in a later phase.").foregroundStyle(.secondary)
            }
        }
        .padding()
    }
}
