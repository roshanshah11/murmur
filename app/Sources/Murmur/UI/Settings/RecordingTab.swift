import SwiftUI

struct RecordingTab: View {
    var body: some View {
        Form {
            Section("Recording") {
                Text("Coming in a later phase.").foregroundStyle(.secondary)
            }
        }
        .padding()
    }
}
