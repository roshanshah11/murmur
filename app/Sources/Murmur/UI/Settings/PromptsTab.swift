import SwiftUI

struct PromptsTab: View {
    var body: some View {
        Form {
            Section("Prompts") {
                Text("Coming in a later phase.").foregroundStyle(.secondary)
            }
        }
        .padding()
    }
}
