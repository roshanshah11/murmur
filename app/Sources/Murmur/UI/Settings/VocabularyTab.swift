import SwiftUI

struct VocabularyTab: View {
    var body: some View {
        Form {
            Section("Vocabulary") {
                Text("Coming in a later phase.").foregroundStyle(.secondary)
            }
        }
        .padding()
    }
}
